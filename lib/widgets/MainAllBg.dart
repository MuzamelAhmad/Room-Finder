import 'package:flutter/material.dart';

import 'GridView.dart';



class MainAllBg extends StatelessWidget {
  const MainAllBg({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
        height: 500,
        decoration: const BoxDecoration(
            color: Colors.white),

    );
  }
}
